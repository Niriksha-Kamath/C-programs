Check if entered number is even or odd
#include<stdio.h>
int isEvenOdd(int number){
  return(number%2==0);
}
int main(){
  isEvenOdd(number);
  return 0;
}
-----------------------------------
Check if entered year is leap year or not
#include<stdio.h>
int checkleap(int year){
  return ((year%4==0 && year%100!=0)||(year%400==0));
}
int main(){
  checkleap(year);
  return 0;
}
------------------------------------
Write a program to convert temperature from Celsius to Fahrenheit 
#include<stdio.h>
int conversion(int celcius){
  return ((9/5*celcius)+32);
}
int main(){
  conversion(celcius);
  return 0;
}
------------------------------------------------------
Write a program to calculate the area of Circle
#include<stdio.h>
#define pie 3.142
float Area(int radius){
  return (pie*radius*radius);
}
int main()
{
  Area(radius);
  return 0;
}
-------------------------------------------------------
Write a program that converts upper case to lower.
#include<stdio.h>
char toLower(char character){
  if(character>='A' && character<="Z"){
    return character+32;
  else
    return character;
  }
int main(){
    toLower(character);
    return 0;
}
------------------------------------------------
Given two character inputs, find number of characters between them. For example, if 
input is ‘A’ and ‘E’ the output is 3.
#include<stdio.h>
int alphabet_difference(char ch1,char ch2){
  difference=abs(ch1-ch2);
  return difference;
}
int main(){
  alphabet_difference(char ch1,char ch2);
  return 0;
}
----------------------------------------------------
WAP to find power of 2^N using left shift operator.
#include<stdio.h>
int two_power_n(int num){
  int res;
  res=1<<num;
  return res;
}
int main(){
  two_power_n(num);
  return 0;
}
-----------------------------------------------
Check if given input is a character or integer.
#include<stdio.h>
int integeroralpha(string s){
  if(s>='0' && s<='9'){
      return 1;
  else if((s>='A' && s<='Z')||(s>='a' && s<='z')){
      return 0;
return -1;
}
int main(){
  integeroralpha(s);
  return 0;
}
-------------------------------------------------
Generate two random numbers and find its sum 
#include<stdio.h>
int randomnumber(int sum){
    int num1=rand()%100;
    int num2=rand()%100;
    sum=num1+num2;
    return sum;
}
int main(){
  randomnumber(sum);
  return 0;
}
-------------------------------------------------------------














  
